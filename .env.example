# OpenAI API key — used for embeddings (text-embedding-3-small) and generation (gpt-4o-mini)
OPENAI_API_KEY=your_openai_api_key_here

# Cohere API key — used for reranking (rerank-english-v3.0)
COHERE_API_KEY=your_cohere_api_key_here
